Algoritmo p2
	//se iguala directamente al valor designado
	x<- 2
	Escribir "X=", X
	//se realiza la expresión aritmética respetando la regla de prioridad
	x<-   (x+x)^2
	Escribir "(x+x)^2=", x
	x<- raiz(x+ raiz(x)+5)
	escribir "raiz(x+ raiz(x)+5)=",x

FinAlgoritmo
